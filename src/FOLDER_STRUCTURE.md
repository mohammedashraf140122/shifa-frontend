# 📁 هيكل المجلدات - Shifa Frontend

## البنية الحالية المنظمة

```
src/
├── 📁 app/                    # إعدادات التطبيق
│   ├── providers/              # Context Providers (فارغ حالياً)
│   └── router/                # Routing
│       ├── AppRouter.jsx       # Routes الرئيسية
│       └── ProtectedLayout.jsx # Layout للمسارات المحمية
│
├── 📁 assets/                  # الملفات الثابتة
│   ├── images/                 # الصور
│   └── svg/                    # SVG Icons
│
├── 📁 components/              # مكونات عامة قابلة لإعادة الاستخدام (فارغ حالياً)
│
├── 📁 config/                  # ملفات الإعدادات
│   └── system-modules.config.js # إعدادات Modules النظام
│
├── 📁 context/                 # React Contexts
│   └── AuthContext.jsx         # Context للمصادقة والصلاحيات
│
├── 📁 core/                    # الوظائف الأساسية (Core Logic)
│   ├── api/                    # API Calls
│   │   ├── auth.js             # API calls للمصادقة
│   │   └── axios.js            # Axios instance وإعدادات
│   ├── auth/                   # منطق المصادقة (فارغ حالياً)
│   ├── permissions/            # منطق الصلاحيات (فارغ حالياً)
│   ├── roles/                 # منطق الأدوار (فارغ حالياً)
│   └── utils/                 # Utility Functions الأساسية
│       ├── auth.js             # وظائف المصادقة
│       ├── token.js            # إدارة Tokens
│       └── menuBuilder.js      # بناء القائمة من API ⭐ (منقول)
│
├── 📁 css/                     # ملفات CSS
│   ├── index.css               # CSS العام + Tailwind Theme
│   └── login.css               # CSS لصفحة Login
│
├── 📁 hooks/                   # Custom React Hooks
│   ├── useLogin.js             # Hook للـ Login
│   ├── useLogout.js            # Hook للـ Logout
│   └── usePermission.js        # Hook للتحقق من الصلاحيات
│
├── 📁 modules/                 # وحدات التطبيق (Features)
│   ├── 404/                    # صفحة 404
│   ├── Admin Settings/         # إعدادات الأدمن
│   ├── Alerts/                 # التنبيهات
│   ├── auth/                   # المصادقة
│   ├── Call Center/           # الكول سنتر
│   ├── chat/                   # الشات
│   ├── Contact/                # التواصل
│   ├── dashboard/              # لوحة التحكم
│   ├── Payroll/                # الرواتب
│   └── Recruitment/           # التوظيف
│
├── 📁 ui/                      # مكونات الواجهة (UI Components)
│   ├── BUTTONS/                # الأزرار
│   ├── card/                   # البطاقات
│   ├── chat-widget/            # Widget الشات
│   ├── header/                 # Header
│   ├── input/                  # حقول الإدخال
│   ├── layout/                 # Layouts
│   │   └── mainlayout.jsx      # Layout الرئيسي
│   └── sidebar/                # Sidebar
│       └── sidebar.jsx         # Sidebar Component
│
├── 📁 validation/              # Validation Schemas
│   └── login.schema.js         # Schema للـ Login
│
├── main.jsx                    # نقطة الدخول الرئيسية
└── App.css                     # CSS للتطبيق (فارغ)
```

---

## 📋 ملاحظات التنظيم

### ✅ الملفات المنظمة:

1. **`core/utils/menuBuilder.js`** ✅
   - تم نقله من `src/utils/` إلى `src/core/utils/`
   - السبب: utility function متعلق بالـ core logic

2. **`core/utils/`** ✅
   - يحتوي على: `auth.js`, `token.js`, `menuBuilder.js`
   - جميع الـ utilities الأساسية في مكان واحد

### 📝 توصيات للمستقبل:

1. **`src/components/`** - يمكن استخدامه للمكونات العامة مثل:
   - ErrorBoundary
   - LoadingSpinner
   - EmptyState
   - etc.

2. **`src/utils/`** - يمكن استخدامه للـ utilities العامة (غير core):
   - formatters
   - helpers
   - constants
   - etc.

3. **`src/core/auth/`** - يمكن استخدامه لوظائف المصادقة المتقدمة

4. **`src/core/permissions/`** - يمكن استخدامه لمنطق الصلاحيات المتقدم

---

## 🔄 التغييرات المنفذة:

- ✅ نقل `menuBuilder.js` من `src/utils/` إلى `src/core/utils/`
- ✅ تحديث import في `sidebar.jsx`
- ✅ حذف الملف القديم

---

**آخر تحديث**: ${new Date().toLocaleDateString('ar-SA')}



